export type TSupport = {
    _id: string;
    name: string;
    email: string;
    phone?: string;
    issueType: string;
    issueDescription: string;
    isDeleted?: boolean;
    createdAt: string;
    updatedAt: string;
};